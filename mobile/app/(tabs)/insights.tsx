import { DestinationScreen } from "@/components/destination-screen";

export default function InsightsScreen() {
  return (
    <DestinationScreen
      title="Insights"
      description="Simple spending insights will become available as your receipt history grows."
      icon="chart.bar.fill"
    />
  );
}
